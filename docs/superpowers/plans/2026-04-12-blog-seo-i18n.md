# Blog with SEO & Multilingual i18n — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a multilingual blog (6 languages) with full SEO to an existing Astro 4.15 portfolio site.

**Architecture:** Astro Content Collections for Markdown posts, `[locale]` dynamic routing for i18n, JSON-LD structured data, hreflang alternate links, RSS per locale, client-side search. RTL support for Arabic via `dir="rtl"` + Tailwind `rtl:` prefix.

**Tech Stack:** Astro 4.15, Tailwind CSS 3.4, TypeScript, @astrojs/sitemap, @astrojs/rss, Bun

---

## File Structure

### New Files
```
src/
  content/
    config.ts
    blog/en/hello-world.md
    blog/ru/hello-world.md
    blog/zh/hello-world.md
    blog/ko/hello-world.md
    blog/ja/hello-world.md
    blog/ar/hello-world.md
  i18n/
    config.ts
    ui.ts
    utils.ts
  layouts/
    BlogPost.astro
  components/
    LanguageSelector.astro
    BlogCard.astro
    TagList.astro
    Search.astro
    TableOfContents.astro
    TranslationLinks.astro
    SEO.astro
  pages/
    [locale]/
      index.astro
      blog/
        index.astro
        [slug].astro
        tags/
          index.astro
          [tag].astro
        feed.xml.ts
public/
  robots.txt
```

### Modified Files
```
astro.config.mjs                     # +sitemap, +site URL
src/layouts/Layout.astro              # +lang, +dir props
src/components/Nav.astro              # +LanguageSelector, +Blog link
src/pages/index.astro                 # redirect to /en/
tailwind.config.mjs                   # +RTL content paths
package.json                          # +@astrojs/sitemap, +@astrojs/rss
```

---

## Task 1: Install Dependencies & Configure Astro

**Files:**
- Modify: `package.json`
- Modify: `astro.config.mjs`

- [ ] **Step 1: Install @astrojs/sitemap and @astrojs/rss**

Run: `cd /Users/suenot/projects/w_sdvg/w_suenot/suenot.github.io && bun add @astrojs/sitemap @astrojs/rss`

- [ ] **Step 2: Update astro.config.mjs**

Replace `astro.config.mjs` with:

```js
import { defineConfig } from 'astro/config';
import tailwind from '@astrojs/tailwind';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://suenot.github.io',
  integrations: [tailwind(), sitemap()],
  build: {
    inlineStylesheets: 'always',
  },
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'ru', 'zh', 'ko', 'ja', 'ar'],
    routing: {
      prefixDefaultLocale: true,
    },
  },
});
```

- [ ] **Step 3: Commit**

```bash
git add package.json bun.lock astro.config.mjs
git commit -m "feat: add sitemap + RSS deps, configure i18n in Astro"
```

---

## Task 2: i18n Configuration & Utilities

**Files:**
- Create: `src/i18n/config.ts`
- Create: `src/i18n/ui.ts`
- Create: `src/i18n/utils.ts`

- [ ] **Step 1: Create src/i18n/config.ts**

```typescript
export const defaultLocale = 'en';

export const locales = ['en', 'ru', 'zh', 'ko', 'ja', 'ar'] as const;

export type Locale = (typeof locales)[number];

export const rtlLocales: Locale[] = ['ar'];

export const localeNames: Record<Locale, string> = {
  en: 'English',
  ru: 'Русский',
  zh: '中文',
  ko: '한국어',
  ja: '日本語',
  ar: 'العربية',
};

export function isRtl(locale: Locale): boolean {
  return rtlLocales.includes(locale);
}
```

- [ ] **Step 2: Create src/i18n/ui.ts**

```typescript
import type { Locale } from './config';

export const ui = {
  en: {
    'nav.about': 'About',
    'nav.tech': 'Tech',
    'nav.projects': 'Projects',
    'nav.contact': 'Contact',
    'nav.blog': 'Blog',
    'blog.title': 'Blog',
    'blog.description': 'Thoughts on development, algorithmic trading, and building products.',
    'blog.readMore': 'Read more',
    'blog.allPosts': 'All Posts',
    'blog.tags': 'Tags',
    'blog.allTags': 'All Tags',
    'blog.postsTagged': 'Posts tagged',
    'blog.backToBlog': '← Back to Blog',
    'blog.translations': 'Translations',
    'blog.search': 'Search posts...',
    'blog.noResults': 'No posts found.',
    'blog.toc': 'Table of Contents',
    'blog.published': 'Published',
    'blog.updated': 'Updated',
    'footer.tagline': '// trading since 2011 · still backtesting',
  },
  ru: {
    'nav.about': 'Обо мне',
    'nav.tech': 'Технологии',
    'nav.projects': 'Проекты',
    'nav.contact': 'Контакты',
    'nav.blog': 'Блог',
    'blog.title': 'Блог',
    'blog.description': 'Мысли о разработке, алгоритмической торговле и создании продуктов.',
    'blog.readMore': 'Читать далее',
    'blog.allPosts': 'Все посты',
    'blog.tags': 'Теги',
    'blog.allTags': 'Все теги',
    'blog.postsTagged': 'Посты с тегом',
    'blog.backToBlog': '← Назад к блогу',
    'blog.translations': 'Переводы',
    'blog.search': 'Поиск постов...',
    'blog.noResults': 'Посты не найдены.',
    'blog.toc': 'Содержание',
    'blog.published': 'Опубликовано',
    'blog.updated': 'Обновлено',
    'footer.tagline': '// торгую с 2011 · всё ещё бэктестирую',
  },
  zh: {
    'nav.about': '关于',
    'nav.tech': '技术栈',
    'nav.projects': '项目',
    'nav.contact': '联系',
    'nav.blog': '博客',
    'blog.title': '博客',
    'blog.description': '关于开发、算法交易和产品构建的思考。',
    'blog.readMore': '阅读更多',
    'blog.allPosts': '所有文章',
    'blog.tags': '标签',
    'blog.allTags': '所有标签',
    'blog.postsTagged': '标签文章',
    'blog.backToBlog': '← 返回博客',
    'blog.translations': '翻译',
    'blog.search': '搜索文章...',
    'blog.noResults': '未找到文章。',
    'blog.toc': '目录',
    'blog.published': '发布于',
    'blog.updated': '更新于',
    'footer.tagline': '// 自2011年交易 · 仍在回测',
  },
  ko: {
    'nav.about': '소개',
    'nav.tech': '기술 스택',
    'nav.projects': '프로젝트',
    'nav.contact': '연락처',
    'nav.blog': '블로그',
    'blog.title': '블로그',
    'blog.description': '개발, 알고리즘 트레이딩 및 제품 구축에 대한 생각.',
    'blog.readMore': '더 읽기',
    'blog.allPosts': '모든 글',
    'blog.tags': '태그',
    'blog.allTags': '모든 태그',
    'blog.postsTagged': '태그된 글',
    'blog.backToBlog': '← 블로그로 돌아가기',
    'blog.translations': '번역',
    'blog.search': '글 검색...',
    'blog.noResults': '글을 찾을 수 없습니다.',
    'blog.toc': '목차',
    'blog.published': '게시일',
    'blog.updated': '수정일',
    'footer.tagline': '// 2011년부터 트레이딩 · 아직도 백테스팅',
  },
  ja: {
    'nav.about': '概要',
    'nav.tech': '技術スタック',
    'nav.projects': 'プロジェクト',
    'nav.contact': '連絡先',
    'nav.blog': 'ブログ',
    'blog.title': 'ブログ',
    'blog.description': '開発、アルゴリズムトレーディング、プロダクト構築に関する考察。',
    'blog.readMore': '続きを読む',
    'blog.allPosts': 'すべての記事',
    'blog.tags': 'タグ',
    'blog.allTags': 'すべてのタグ',
    'blog.postsTagged': 'タグ付き記事',
    'blog.backToBlog': '← ブログに戻る',
    'blog.translations': '翻訳',
    'blog.search': '記事を検索...',
    'blog.noResults': '記事が見つかりません。',
    'blog.toc': '目次',
    'blog.published': '公開日',
    'blog.updated': '更新日',
    'footer.tagline': '// 2011年からトレーディング · まだバックテスト中',
  },
  ar: {
    'nav.about': 'عن',
    'nav.tech': 'التقنيات',
    'nav.projects': 'المشاريع',
    'nav.contact': 'اتصل',
    'nav.blog': 'المدونة',
    'blog.title': 'المدونة',
    'blog.description': 'أفكار حول التطوير والتداول الخوارزمي وبناء المنتجات.',
    'blog.readMore': 'اقرأ المزيد',
    'blog.allPosts': 'جميع المنشورات',
    'blog.tags': 'الوسوم',
    'blog.allTags': 'جميع الوسوم',
    'blog.postsTagged': 'منشورات موسومة',
    'blog.backToBlog': '→ العودة إلى المدونة',
    'blog.translations': 'الترجمات',
    'blog.search': 'بحث المنشورات...',
    'blog.noResults': 'لم يتم العثور على منشورات.',
    'blog.toc': 'جدول المحتويات',
    'blog.published': 'نُشر',
    'blog.updated': 'مُحدّث',
    'footer.tagline': '// يتداول منذ 2011 · لا يزال يختبر',
  },
} as const;

export type UIKey = keyof (typeof ui)['en'];

export function useTranslations(locale: Locale) {
  return function t(key: UIKey): string {
    return ui[locale]?.[key] || ui['en'][key] || key;
  };
}
```

- [ ] **Step 3: Create src/i18n/utils.ts**

```typescript
import { defaultLocale, locales, type Locale } from './config';

export function getLangFromUrl(url: URL): Locale {
  const [, locale] = url.pathname.split('/');
  if (locales.includes(locale as Locale)) {
    return locale as Locale;
  }
  return defaultLocale;
}

export function getLocalizedPath(path: string, locale: Locale): string {
  return `/${locale}${path}`;
}

export function getLocaleFromSlug(slug: string): Locale {
  // slug format: "en/my-post" or just "my-post"
  return defaultLocale;
}

export function getStaticLocalePaths() {
  return locales.map((locale) => ({ params: { locale } }));
}
```

- [ ] **Step 4: Commit**

```bash
git add src/i18n/
git commit -m "feat: add i18n config, UI translations, and utility functions"
```

---

## Task 3: Content Collections Setup

**Files:**
- Create: `src/content/config.ts`

- [ ] **Step 1: Create src/content/config.ts**

```typescript
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

const blog = defineCollection({
  loader: glob({ pattern: '**/*.md', base: './src/content/blog' }),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    pubDate: z.coerce.date(),
    updatedDate: z.coerce.date().optional(),
    heroImage: z.string().optional(),
    tags: z.array(z.string()).default([]),
    draft: z.boolean().default(false),
  }),
});

export const collections = { blog };
```

- [ ] **Step 2: Commit**

```bash
git add src/content/
git commit -m "feat: add blog content collection with Zod schema"
```

---

## Task 4: Demo Blog Posts (6 Languages)

**Files:**
- Create: `src/content/blog/en/hello-world.md`
- Create: `src/content/blog/ru/hello-world.md`
- Create: `src/content/blog/zh/hello-world.md`
- Create: `src/content/blog/ko/hello-world.md`
- Create: `src/content/blog/ja/hello-world.md`
- Create: `src/content/blog/ar/hello-world.md`

- [ ] **Step 1: Create English demo post at src/content/blog/en/hello-world.md**

```markdown
---
title: "Hello World"
description: "First post on the blog. Building algorithmic trading platforms, HFT infrastructure, and AI-powered developer tools."
pubDate: 2026-04-12
tags: ["intro", "trading"]
draft: false
---

Welcome to my blog. I've been trading since 2011 and building developer tools for over 15 years.

## What to expect

This blog will cover:

- **Algorithmic trading** — strategies, backtesting, and live trading systems
- **Developer tooling** — building CLI tools, static site generators, and automation
- **Infrastructure** — HFT systems, low-latency networking, and deployment pipelines

Stay tuned for more posts.
```

- [ ] **Step 2: Create Russian demo post at src/content/blog/ru/hello-world.md**

```markdown
---
title: "Привет, мир"
description: "Первый пост в блоге. Алгоритмическая торговля, HFT-инфраструктура и инструменты для разработчиков."
pubDate: 2026-04-12
tags: ["intro", "trading"]
draft: false
---

Добро пожаловать в мой блог. Я торгую с 2011 года и создаю инструменты для разработчиков более 15 лет.

## Что ожидать

В этом блоге будут публиковаться материалы о:

- **Алгоритмической торговле** — стратегии, бэктестинг и системы живой торговли
- **Инструментах разработчика** — CLI-утилиты, генераторы статических сайтов, автоматизация
- **Инфраструктуре** — HFT-системы, низколатентные сети и пайплайны развёртывания

Следите за обновлениями.
```

- [ ] **Step 3: Create Chinese demo post at src/content/blog/zh/hello-world.md**

```markdown
---
title: "你好世界"
description: "博客的第一篇文章。构建算法交易平台、HFT基础设施和AI驱动的开发者工具。"
pubDate: 2026-04-12
tags: ["intro", "trading"]
draft: false
---

欢迎来到我的博客。我从2011年开始交易，构建开发者工具超过15年。

## 期待什么

本博客将涵盖：

- **算法交易** — 策略、回测和实盘交易系统
- **开发者工具** — 构建CLI工具、静态网站生成器和自动化
- **基础设施** — HFT系统、低延迟网络和部署管道

请继续关注更多文章。
```

- [ ] **Step 4: Create Korean demo post at src/content/blog/ko/hello-world.md**

```markdown
---
title: "헬로 월드"
description: "블로그의 첫 번째 글입니다. 알고리즘 트레이딩 플랫폼, HFT 인프라 및 AI 기반 개발자 도구 구축."
pubDate: 2026-04-12
tags: ["intro", "trading"]
draft: false
---

제 블로그에 오신 것을 환영합니다. 저는 2011년부터 트레이딩을 시작했고 15년 넘게 개발자 도구를 만들어왔습니다.

## 기대할 수 있는 내용

이 블로그에서 다룰 주제:

- **알고리즘 트레이딩** — 전략, 백테스팅, 라이브 트레이딩 시스템
- **개발자 도구** — CLI 도구, 정적 사이트 생성기, 자동화
- **인프라** — HFT 시스템, 저지연 네트워킹, 배포 파이프라인

더 많은 글을 기대해 주세요.
```

- [ ] **Step 5: Create Japanese demo post at src/content/blog/ja/hello-world.md**

```markdown
---
title: "こんにちは世界"
description: "ブログの最初の投稿。アルゴリズムトレーディングプラットフォーム、HFTインフラ、AI駆動開発ツールの構築。"
pubDate: 2026-04-12
tags: ["intro", "trading"]
draft: false
---

ブログへようこそ。2011年からトレーディングを始め、15年以上開発者ツールを構築しています。

## 期待できる内容

このブログでは以下を扱います：

- **アルゴリズムトレーディング** — 戦略、バックテスト、ライブトレーディングシステム
- **開発者ツール** — CLIツール、静的サイトジェネレーター、自動化
- **インフラ** — HFTシステム、低遅延ネットワーク、デプロイパイプライン

今後の投稿をお楽しみに。
```

- [ ] **Step 6: Create Arabic demo post at src/content/blog/ar/hello-world.md**

```markdown
---
title: "مرحبا بالعالم"
description: "أول منشور على المدونة. بناء منصات التداول الخوارزمي والبنية التحتية HFT وأدوات المطورين المدعومة بالذكاء الاصطناعي."
pubDate: 2026-04-12
tags: ["intro", "trading"]
draft: false
---

مرحباً بكم في مدونتي. أتداول منذ عام 2011 وأبني أدوات للمطورين منذ أكثر من 15 عاماً.

## ما يمكن توقعه

ستغطي هذه المدونة:

- **التداول الخوارزمي** — الاستراتيجيات والاختبار الخلفي وأنظمة التداول المباشر
- **أدوات المطورين** — بناء أدوات سطر الأوامر ومولدات المواقع الثابتة والأتمتة
- **البنية التحتية** — أنظمة HFT والشبكات منخفضة الكمون وخطوط النشر

ترقبوا المزيد من المنشورات.
```

- [ ] **Step 7: Commit**

```bash
git add src/content/blog/
git commit -m "feat: add demo blog post in 6 languages"
```

---

## Task 5: SEO Component

**Files:**
- Create: `src/components/SEO.astro`

- [ ] **Step 1: Create src/components/SEO.astro**

```astro
---
import { locales, defaultLocale, type Locale } from '../i18n/config';

interface Props {
  title: string;
  description: string;
  locale: Locale;
  canonicalUrl?: string;
  ogImage?: string;
  type?: 'website' | 'article';
  publishedDate?: Date;
  modifiedDate?: Date;
  tags?: string[];
}

const {
  title,
  description,
  locale,
  canonicalUrl,
  ogImage,
  type = 'website',
  publishedDate,
  modifiedDate,
  tags = [],
} = Astro.props;

const siteUrl = Astro.site?.toString().replace(/\/$/, '') || 'https://suenot.github.io';
const currentUrl = canonicalUrl || new URL(Astro.url.pathname, siteUrl).toString();
const imageUrl = ogImage ? new URL(ogImage, siteUrl).toString() : '';

// Build hreflang links
const hreflangLinks = locales.map((loc) => {
  const path = Astro.url.pathname.replace(`/${locale}`, `/${loc}`);
  return { locale: loc, url: `${siteUrl}${path}` };
});
// x-default points to default locale
const xDefaultPath = Astro.url.pathname.replace(`/${locale}`, `/${defaultLocale}`);

const jsonLd = type === 'article' && publishedDate
  ? {
      '@context': 'https://schema.org',
      '@type': 'BlogPosting',
      headline: title,
      description,
      url: currentUrl,
      datePublished: publishedDate.toISOString(),
      ...(modifiedDate && { dateModified: modifiedDate.toISOString() }),
      ...(imageUrl && { image: imageUrl }),
      ...(tags.length > 0 && { keywords: tags.join(', ') }),
      author: {
        '@type': 'Person',
        name: 'Eugen Soloviov',
        url: siteUrl,
      },
    }
  : {
      '@context': 'https://schema.org',
      '@type': 'WebSite',
      name: 'Eugen Soloviov',
      url: siteUrl,
    };
---

<!-- Canonical -->
<link rel="canonical" href={currentUrl} />

<!-- Hreflang -->
{hreflangLinks.map(({ locale: loc, url }) => (
  <link rel="alternate" hreflang={loc} href={url} />
))}
<link rel="alternate" hreflang="x-default" href={`${siteUrl}${xDefaultPath}`} />

<!-- Open Graph -->
<meta property="og:type" content={type} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:url" content={currentUrl} />
<meta property="og:locale" content={locale} />
{locales.filter((l) => l !== locale).map((loc) => (
  <meta property="og:locale:alternate" content={loc} />
))}
{imageUrl && <meta property="og:image" content={imageUrl} />}

<!-- Twitter Card -->
<meta name="twitter:card" content={imageUrl ? 'summary_large_image' : 'summary'} />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
{imageUrl && <meta name="twitter:image" content={imageUrl} />}

<!-- JSON-LD -->
<script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
```

- [ ] **Step 2: Commit**

```bash
git add src/components/SEO.astro
git commit -m "feat: add SEO component with hreflang, OG, Twitter Cards, JSON-LD"
```

---

## Task 6: Update Layout

**Files:**
- Modify: `src/layouts/Layout.astro`

- [ ] **Step 1: Replace src/layouts/Layout.astro**

```astro
---
import SEO from '../components/SEO.astro';
import { type Locale, isRtl, defaultLocale, locales } from '../i18n/config';

interface Props {
  title?: string;
  description?: string;
  locale?: Locale;
  ogImage?: string;
  type?: 'website' | 'article';
  publishedDate?: Date;
  modifiedDate?: Date;
  tags?: string[];
}

const {
  title = 'Eugen Soloviov — Fullstack & Algotrading Developer',
  description = 'Co-founder & CTO of Reforms.ai. Building algorithmic trading platforms, HFT infrastructure, and AI-powered tools.',
  locale = defaultLocale,
  ogImage,
  type = 'website',
  publishedDate,
  modifiedDate,
  tags,
} = Astro.props;

const dir = isRtl(locale) ? 'rtl' : 'ltr';
---

<!doctype html>
<html lang={locale} dir={dir} class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <SEO
      title={title}
      description={description}
      locale={locale}
      ogImage={ogImage}
      type={type}
      publishedDate={publishedDate}
      modifiedDate={modifiedDate}
      tags={tags}
    />
  </head>
  <body class="bg-[#0A0A14] text-gray-100 font-sans antialiased">
    <slot />
  </body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add src/layouts/Layout.astro
git commit -m "feat: update Layout with locale, RTL, SEO integration"
```

---

## Task 7: Blog Components

**Files:**
- Create: `src/components/BlogCard.astro`
- Create: `src/components/TagList.astro`
- Create: `src/components/TableOfContents.astro`
- Create: `src/components/TranslationLinks.astro`
- Create: `src/components/Search.astro`

- [ ] **Step 1: Create src/components/BlogCard.astro**

```astro
---
interface Props {
  title: string;
  description: string;
  pubDate: Date;
  tags: string[];
  heroImage?: string;
  slug: string;
  readMoreLabel: string;
}

const { title, description, pubDate, tags, heroImage, slug, readMoreLabel } = Astro.props;

const formattedDate = pubDate.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---

<article class="group rounded-xl bg-white/[0.03] border border-white/5 overflow-hidden hover:border-violet-500/20 transition-all duration-300">
  {heroImage && (
    <div class="aspect-video bg-surface-2 overflow-hidden">
      <img src={heroImage} alt={title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300" loading="lazy" />
    </div>
  )}
  <div class="p-5">
    <time datetime={pubDate.toISOString()} class="text-xs text-gray-500 font-mono">{formattedDate}</time>
    <h3 class="text-lg font-semibold text-white mt-2 mb-2 group-hover:text-violet-300 transition-colors">
      <a href={slug} class="hover:underline">{title}</a>
    </h3>
    <p class="text-sm text-gray-400 leading-relaxed mb-4">{description}</p>
    <div class="flex items-center justify-between">
      <div class="flex flex-wrap gap-1.5">
        {tags.slice(0, 3).map((tag) => (
          <span class="px-2 py-0.5 text-xs rounded-full bg-violet-500/10 text-violet-300 border border-violet-500/20">{tag}</span>
        ))}
      </div>
      <span class="text-xs text-violet-400 font-medium group-hover:text-violet-300">{readMoreLabel} →</span>
    </div>
  </div>
</article>
```

- [ ] **Step 2: Create src/components/TagList.astro**

```astro
---
interface Props {
  tags: string[];
  locale: string;
  activeTag?: string;
}

const { tags, locale, activeTag } = Astro.props;

const uniqueTags = [...new Set(tags)].sort();
---

<div class="flex flex-wrap gap-2">
  <a
    href={`/${locale}/blog/`}
    class:list={[
      'px-3 py-1.5 text-sm rounded-full border transition-colors duration-200',
      !activeTag
        ? 'bg-violet-500/20 text-violet-300 border-violet-500/30'
        : 'bg-white/[0.03] text-gray-400 border-white/5 hover:border-violet-500/20 hover:text-white',
    ]}
  >
    All
  </a>
  {uniqueTags.map((tag) => (
    <a
      href={`/${locale}/blog/tags/${tag}/`}
      class:list={[
        'px-3 py-1.5 text-sm rounded-full border transition-colors duration-200',
        activeTag === tag
          ? 'bg-violet-500/20 text-violet-300 border-violet-500/30'
          : 'bg-white/[0.03] text-gray-400 border-white/5 hover:border-violet-500/20 hover:text-white',
      ]}
    >
      {tag}
    </a>
  ))}
</div>
```

- [ ] **Step 3: Create src/components/TableOfContents.astro**

```astro
---
interface Heading {
  depth: number;
  slug: string;
  text: string;
}

interface Props {
  headings: Heading[];
  title: string;
}

const { headings, title } = Astro.props;
// Only show h2 and h3
const tocHeadings = headings.filter((h) => h.depth >= 2 && h.depth <= 3);
---

{tocHeadings.length > 0 && (
  <nav class="hidden lg:block sticky top-24 ml-8 w-56 shrink-0" aria-label={title}>
    <h4 class="text-sm font-semibold text-gray-400 mb-3 font-mono">{title}</h4>
    <ul class="space-y-2 text-sm">
      {tocHeadings.map((heading) => (
        <li class={heading.depth === 3 ? 'pl-3' : ''}>
          <a
            href={`#${heading.slug}`}
            class="text-gray-500 hover:text-violet-300 transition-colors duration-200 leading-snug block"
          >
            {heading.text}
          </a>
        </li>
      ))}
    </ul>
  </nav>
)}
```

- [ ] **Step 4: Create src/components/TranslationLinks.astro**

```astro
---
import { locales, localeNames, type Locale } from '../i18n/config';

interface Props {
  currentLocale: Locale;
  slug: string;
  availableLocales: Locale[];
  label: string;
}

const { currentLocale, slug, availableLocales, label } = Astro.props;

const otherLocales = availableLocales.filter((l) => l !== currentLocale);
---

{otherLocales.length > 0 && (
  <div class="flex items-center gap-3 text-sm">
    <span class="text-gray-500">{label}:</span>
    {otherLocales.map((loc) => (
      <a
        href={`/${loc}/blog/${slug}/`}
        class="px-2 py-1 rounded bg-white/[0.03] border border-white/5 text-gray-400 hover:text-violet-300 hover:border-violet-500/20 transition-colors"
      >
        {localeNames[loc]}
      </a>
    ))}
  </div>
)}
```

- [ ] **Step 5: Create src/components/Search.astro**

```astro
---
interface Props {
  placeholder: string;
  noResultsText: string;
}

const { placeholder, noResultsText } = Astro.props;
---

<div class="relative">
  <input
    type="search"
    id="blog-search"
    placeholder={placeholder}
    class="w-full px-4 py-2.5 rounded-lg bg-white/[0.03] border border-white/10 text-gray-200 placeholder-gray-500 focus:outline-none focus:border-violet-500/40 focus:ring-1 focus:ring-violet-500/20 transition-colors text-sm"
  />
  <div id="search-results" class="mt-2"></div>
</div>

<script>
  interface PostData {
    title: string;
    description: string;
    slug: string;
    tags: string[];
    pubDate: string;
  }

  const input = document.getElementById('blog-search') as HTMLInputElement;
  const container = document.getElementById('blog-search-container');
  const noResultsText = container?.dataset.noResults || 'No posts found.';

  // Collect post data from the DOM
  function getPosts(): PostData[] {
    const cards = document.querySelectorAll('[data-blog-post]');
    return Array.from(cards).map((card) => ({
      title: (card as HTMLElement).dataset.title || '',
      description: (card as HTMLElement).dataset.description || '',
      slug: (card as HTMLElement).dataset.slug || '',
      tags: (card as HTMLElement).dataset.tags?.split(',') || [],
      pubDate: (card as HTMLElement).dataset.pubdate || '',
    }));
  }

  function showAll() {
    document.querySelectorAll('[data-blog-post]').forEach((el) => {
      (el as HTMLElement).style.display = '';
    });
    const noResults = document.getElementById('no-results');
    if (noResults) noResults.style.display = 'none';
  }

  function filterPosts(query: string) {
    if (!query.trim()) {
      showAll();
      return;
    }

    const q = query.toLowerCase();
    const posts = getPosts();
    let visibleCount = 0;

    document.querySelectorAll('[data-blog-post]').forEach((el, i) => {
      const post = posts[i];
      const match =
        post.title.toLowerCase().includes(q) ||
        post.description.toLowerCase().includes(q) ||
        post.tags.some((t) => t.toLowerCase().includes(q));
      (el as HTMLElement).style.display = match ? '' : 'none';
      if (match) visibleCount++;
    });

    const noResults = document.getElementById('no-results');
    if (noResults) {
      noResults.style.display = visibleCount === 0 ? 'block' : 'none';
    }
  }

  input?.addEventListener('input', (e) => {
    filterPosts((e.target as HTMLInputElement).value);
  });
</script>
```

- [ ] **Step 6: Commit**

```bash
git add src/components/BlogCard.astro src/components/TagList.astro src/components/TableOfContents.astro src/components/TranslationLinks.astro src/components/Search.astro
git commit -m "feat: add blog components (BlogCard, TagList, TOC, TranslationLinks, Search)"
```

---

## Task 8: Language Selector Component

**Files:**
- Create: `src/components/LanguageSelector.astro`

- [ ] **Step 1: Create src/components/LanguageSelector.astro**

```astro
---
import { locales, localeNames, type Locale } from '../i18n/config';

interface Props {
  currentLocale: Locale;
}

const { currentLocale } = Astro.props;
---

<div class="relative" id="lang-selector">
  <button
    id="lang-toggle"
    class="flex items-center gap-1.5 text-sm text-gray-400 hover:text-white transition-colors font-mono"
    aria-label="Select language"
    aria-expanded="false"
  >
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" />
    </svg>
    <span>{localeNames[currentLocale]}</span>
    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
    </svg>
  </button>
  <div id="lang-menu" class="hidden absolute right-0 top-full mt-2 py-1 w-40 rounded-lg bg-surface-2 border border-white/10 shadow-xl z-50">
    {locales.map((loc) => (
      <a
        href={Astro.url.pathname.replace(`/${currentLocale}`, `/${loc}`)}
        class:list={[
          'block px-3 py-2 text-sm transition-colors',
          loc === currentLocale
            ? 'text-violet-300 bg-violet-500/10'
            : 'text-gray-400 hover:text-white hover:bg-white/[0.03]',
        ]}
      >
        {localeNames[loc]}
      </a>
    ))}
  </div>
</div>

<script>
  const toggle = document.getElementById('lang-toggle');
  const menu = document.getElementById('lang-menu');

  toggle?.addEventListener('click', () => {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    menu?.classList.toggle('hidden');
  });

  document.addEventListener('click', (e) => {
    const selector = document.getElementById('lang-selector');
    if (selector && !selector.contains(e.target as Node)) {
      toggle?.setAttribute('aria-expanded', 'false');
      menu?.classList.add('hidden');
    }
  });
</script>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/LanguageSelector.astro
git commit -m "feat: add LanguageSelector dropdown component"
```

---

## Task 9: Update Nav Component

**Files:**
- Modify: `src/components/Nav.astro`

- [ ] **Step 1: Replace src/components/Nav.astro**

```astro
---
import LanguageSelector from './LanguageSelector.astro';
import { type Locale } from '../i18n/config';
import { useTranslations } from '../i18n/ui';

interface Props {
  locale?: Locale;
}

const { locale = 'en' } = Astro.props;
const t = useTranslations(locale);

const links = [
  { href: `/${locale}/#about`, label: t('nav.about') },
  { href: `/${locale}/#tech`, label: t('nav.tech') },
  { href: `/${locale}/#projects`, label: t('nav.projects') },
  { href: `/${locale}/#contact`, label: t('nav.contact') },
  { href: `/${locale}/blog/`, label: t('nav.blog') },
];
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-[#0A0A14]/80 backdrop-blur-md border-b border-white/5">
  <div class="max-w-5xl mx-auto px-6 py-4 flex items-center justify-between">
    <a href={`/${locale}/`} class="font-mono text-violet-400 font-semibold text-sm tracking-wider">
      suenot<span class="text-gray-600">.com</span>
    </a>
    <ul class="hidden sm:flex items-center gap-6">
      {links.map((link) => (
        <li>
          <a
            href={link.href}
            class="text-sm text-gray-400 hover:text-white transition-colors duration-200"
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
    <div class="flex items-center gap-4">
      <LanguageSelector currentLocale={locale} />
      <a
        href="https://github.com/suenot"
        target="_blank"
        rel="noopener noreferrer"
        class="text-sm text-violet-400 hover:text-violet-300 transition-colors font-mono hidden sm:inline"
      >
        github →
      </a>
    </div>
  </div>
</nav>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Nav.astro
git commit -m "feat: update Nav with i18n links and language selector"
```

---

## Task 10: Blog Layout

**Files:**
- Create: `src/layouts/BlogPost.astro`

- [ ] **Step 1: Create src/layouts/BlogPost.astro**

```astro
---
import Layout from './Layout.astro';
import TableOfContents from '../components/TableOfContents.astro';
import TranslationLinks from '../components/TranslationLinks.astro';
import TagList from '../components/TagList.astro';
import { type Locale, locales } from '../i18n/config';
import { useTranslations } from '../i18n/ui';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  heroImage?: string;
  tags: string[];
  locale: Locale;
  slug: string;
  availableLocales?: Locale[];
  headings: { depth: number; slug: string; text: string }[];
}

const {
  title,
  description,
  pubDate,
  updatedDate,
  heroImage,
  tags,
  locale,
  slug,
  availableLocales = [],
  headings,
} = Astro.props;

const t = useTranslations(locale);

const formattedPubDate = pubDate.toLocaleDateString(locale === 'en' ? 'en-US' : locale, {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

const formattedUpdatedDate = updatedDate?.toLocaleDateString(locale === 'en' ? 'en-US' : locale, {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

// BreadcrumbList JSON-LD
const breadcrumbLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: `https://suenot.github.io/${locale}/` },
    { '@type': 'ListItem', position: 2, name: 'Blog', item: `https://suenot.github.io/${locale}/blog/` },
    { '@type': 'ListItem', position: 3, name: title },
  ],
};
---

<Layout
  title={`${title} — Eugen Soloviov`}
  description={description}
  locale={locale}
  ogImage={heroImage}
  type="article"
  publishedDate={pubDate}
  modifiedDate={updatedDate}
  tags={tags}
>
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbLd)} />

  <article class="min-h-screen pt-24 pb-16">
    <div class="max-w-5xl mx-auto px-6">
      <!-- Back link -->
      <a
        href={`/${locale}/blog/`}
        class="inline-flex items-center text-sm text-gray-400 hover:text-violet-300 transition-colors mb-8"
      >
        {t('blog.backToBlog')}
      </a>

      {heroImage && (
        <div class="aspect-video rounded-xl overflow-hidden mb-8 bg-surface-2">
          <img src={heroImage} alt={title} class="w-full h-full object-cover" />
        </div>
      )}

      <div class="flex flex-col lg:flex-row gap-8">
        <!-- Main content -->
        <div class="flex-1 min-w-0">
          <header class="mb-8">
            <div class="flex flex-wrap items-center gap-3 mb-4">
              <time datetime={pubDate.toISOString()} class="text-sm text-gray-500 font-mono">
                {t('blog.published')}: {formattedPubDate}
              </time>
              {updatedDate && (
                <time datetime={updatedDate.toISOString()} class="text-sm text-gray-600 font-mono">
                  {t('blog.updated')}: {formattedUpdatedDate}
                </time>
              )}
            </div>
            <h1 class="text-3xl sm:text-4xl font-bold text-white mb-4">{title}</h1>
            <div class="mb-4">
              <TagList tags={tags} locale={locale} />
            </div>
            {availableLocales.length > 1 && (
              <TranslationLinks
                currentLocale={locale}
                slug={slug}
                availableLocales={availableLocales}
                label={t('blog.translations')}
              />
            )}
          </header>

          <!-- Prose content -->
          <div class="prose-container">
            <slot />
          </div>
        </div>

        <!-- Table of Contents -->
        <TableOfContents headings={headings} title={t('blog.toc')} />
      </div>
    </div>
  </article>

  <style>
    .prose-container :global(h2) {
      @apply text-2xl font-bold text-white mt-10 mb-4;
    }
    .prose-container :global(h3) {
      @apply text-xl font-semibold text-white mt-8 mb-3;
    }
    .prose-container :global(h4) {
      @apply text-lg font-semibold text-gray-200 mt-6 mb-2;
    }
    .prose-container :global(p) {
      @apply text-gray-300 leading-relaxed mb-4;
    }
    .prose-container :global(a) {
      @apply text-violet-400 hover:text-violet-300 underline decoration-violet-500/30 hover:decoration-violet-300 transition-colors;
    }
    .prose-container :global(ul), .prose-container :global(ol) {
      @apply text-gray-300 mb-4 pl-6;
    }
    .prose-container :global(ul) {
      @apply list-disc;
    }
    .prose-container :global(ol) {
      @apply list-decimal;
    }
    .prose-container :global(li) {
      @apply mb-1.5;
    }
    .prose-container :global(blockquote) {
      @apply border-l-2 border-violet-500/30 pl-4 text-gray-400 italic my-4;
    }
    .prose-container :global(code) {
      @apply bg-white/[0.05] px-1.5 py-0.5 rounded text-sm font-mono text-violet-300;
    }
    .prose-container :global(pre) {
      @apply bg-surface-2 border border-white/5 rounded-lg p-4 overflow-x-auto my-4;
    }
    .prose-container :global(pre code) {
      @apply bg-transparent px-0 py-0;
    }
    .prose-container :global(img) {
      @apply rounded-lg my-4;
    }
    .prose-container :global(hr) {
      @apply border-white/5 my-8;
    }
    .prose-container :global(table) {
      @apply w-full text-sm my-4;
    }
    .prose-container :global(th) {
      @apply text-left text-gray-400 font-mono py-2 px-3 border-b border-white/10;
    }
    .prose-container :global(td) {
      @apply py-2 px-3 border-b border-white/5;
    }
  </style>
</Layout>
```

- [ ] **Step 2: Commit**

```bash
git add src/layouts/BlogPost.astro
git commit -m "feat: add BlogPost layout with TOC, translations, prose styles"
```

---

## Task 11: Blog Pages (List, Post, Tags)

**Files:**
- Create: `src/pages/[locale]/blog/index.astro`
- Create: `src/pages/[locale]/blog/[slug].astro`
- Create: `src/pages/[locale]/blog/tags/index.astro`
- Create: `src/pages/[locale]/blog/tags/[tag].astro`

- [ ] **Step 1: Create src/pages/[locale]/blog/index.astro**

```astro
---
import type { GetStaticPaths } from 'astro';
import Layout from '../../../layouts/Layout.astro';
import Nav from '../../../components/Nav.astro';
import BlogCard from '../../../components/BlogCard.astro';
import TagList from '../../../components/TagList.astro';
import Search from '../../../components/Search.astro';
import { locales, type Locale } from '../../../i18n/config';
import { useTranslations } from '../../../i18n/ui';
import { getCollection } from 'astro:content';

export const getStaticPaths = (() => {
  return locales.map((locale) => ({ params: { locale } }));
}) satisfies GetStaticPaths;

const { locale } = Astro.params as { locale: Locale };
const t = useTranslations(locale);

const allPosts = await getCollection('blog', ({ data, id }) => {
  return id.startsWith(`${locale}/`) && !data.draft;
});

const posts = allPosts.sort(
  (a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime()
);

const allTags = posts.flatMap((post) => post.data.tags);

// Strip locale prefix from id to get slug
function getSlug(id: string): string {
  return id.replace(`${locale}/`, '').replace(/\.md$/, '');
}
---

<Layout
  title={`${t('blog.title')} — Eugen Soloviov`}
  description={t('blog.description')}
  locale={locale}
>
  <Nav locale={locale} />
  <main class="min-h-screen pt-24 pb-16">
    <div class="max-w-5xl mx-auto px-6">
      <header class="mb-10">
        <h1 class="text-3xl sm:text-4xl font-bold text-white mb-3">{t('blog.title')}</h1>
        <p class="text-gray-400 text-lg">{t('blog.description')}</p>
      </header>

      <div class="mb-6">
        <Search placeholder={t('blog.search')} noResultsText={t('blog.noResults')} />
      </div>

      <div class="mb-8">
        <TagList tags={allTags} locale={locale} />
      </div>

      <div id="blog-search-container" data-no-results={t('blog.noResults')}>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {posts.map((post) => (
            <div data-blog-post data-title={post.data.title} data-description={post.data.description} data-slug={getSlug(post.id)} data-tags={post.data.tags.join(',')} data-pubdate={post.data.pubDate.toISOString()}>
              <BlogCard
                title={post.data.title}
                description={post.data.description}
                pubDate={post.data.pubDate}
                tags={post.data.tags}
                heroImage={post.data.heroImage}
                slug={`/${locale}/blog/${getSlug(post.id)}/`}
                readMoreLabel={t('blog.readMore')}
              />
            </div>
          ))}
        </div>
        <p id="no-results" class="text-center text-gray-500 py-12 hidden">{t('blog.noResults')}</p>
      </div>
    </div>
  </main>
  <footer class="border-t border-white/5 py-8 text-center text-sm text-gray-400">
    <p class="font-mono text-xs tracking-wider">{t('footer.tagline')}</p>
  </footer>
</Layout>
```

- [ ] **Step 2: Create src/pages/[locale]/blog/[slug].astro**

```astro
---
import type { GetStaticPaths } from 'astro';
import BlogPost from '../../../layouts/BlogPost.astro';
import Nav from '../../../components/Nav.astro';
import { locales, type Locale } from '../../../i18n/config';
import { getCollection, render } from 'astro:content';

export const getStaticPaths = (async () => {
  const paths = [];
  for (const locale of locales) {
    const posts = await getCollection('blog', ({ id, data }) => {
      return id.startsWith(`${locale}/`) && !data.draft;
    });
    for (const post of posts) {
      const slug = post.id.replace(`${locale}/`, '').replace(/\.md$/, '');
      paths.push({
        params: { locale, slug },
        props: { post },
      });
    }
  }
  return paths;
}) satisfies GetStaticPaths;

const { locale } = Astro.params as { locale: Locale };
const { post } = Astro.props;

const { Content, headings } = await render(post);

// Check which locales have this post
const slug = post.id.replace(`${locale}/`, '').replace(/\.md$/, '');
const availableLocales: Locale[] = [];
for (const loc of locales) {
  const exists = await getCollection('blog', ({ id }) => id === `${loc}/${slug}.md`);
  if (exists.length > 0) availableLocales.push(loc);
}
---

<Nav locale={locale} />
<BlogPost
  title={post.data.title}
  description={post.data.description}
  pubDate={post.data.pubDate}
  updatedDate={post.data.updatedDate}
  heroImage={post.data.heroImage}
  tags={post.data.tags}
  locale={locale}
  slug={slug}
  availableLocales={availableLocales}
  headings={headings}
>
  <Content />
</BlogPost>
```

- [ ] **Step 3: Create src/pages/[locale]/blog/tags/index.astro**

```astro
---
import type { GetStaticPaths } from 'astro';
import Layout from '../../../../layouts/Layout.astro';
import Nav from '../../../../components/Nav.astro';
import { locales, type Locale } from '../../../../i18n/config';
import { useTranslations } from '../../../../i18n/ui';
import { getCollection } from 'astro:content';

export const getStaticPaths = (() => {
  return locales.map((locale) => ({ params: { locale } }));
}) satisfies GetStaticPaths;

const { locale } = Astro.params as { locale: Locale };
const t = useTranslations(locale);

const allPosts = await getCollection('blog', ({ id, data }) => {
  return id.startsWith(`${locale}/`) && !data.draft;
});

const tagCounts: Record<string, number> = {};
allPosts.forEach((post) => {
  post.data.tags.forEach((tag) => {
    tagCounts[tag] = (tagCounts[tag] || 0) + 1;
  });
});

const sortedTags = Object.entries(tagCounts).sort((a, b) => b[1] - a[1]);
---

<Layout
  title={`${t('blog.tags')} — Eugen Soloviov`}
  description={t('blog.description')}
  locale={locale}
>
  <Nav locale={locale} />
  <main class="min-h-screen pt-24 pb-16">
    <div class="max-w-5xl mx-auto px-6">
      <header class="mb-10">
        <h1 class="text-3xl sm:text-4xl font-bold text-white mb-3">{t('blog.allTags')}</h1>
        <p class="text-gray-400 text-lg">{t('blog.description')}</p>
      </header>

      <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
        {sortedTags.map(([tag, count]) => (
          <a
            href={`/${locale}/blog/tags/${tag}/`}
            class="group p-4 rounded-xl bg-white/[0.03] border border-white/5 hover:border-violet-500/20 transition-all"
          >
            <div class="text-white font-medium group-hover:text-violet-300 transition-colors">#{tag}</div>
            <div class="text-sm text-gray-500 mt-1">{count} {count === 1 ? 'post' : 'posts'}</div>
          </a>
        ))}
      </div>
    </div>
  </main>
  <footer class="border-t border-white/5 py-8 text-center text-sm text-gray-400">
    <p class="font-mono text-xs tracking-wider">{t('footer.tagline')}</p>
  </footer>
</Layout>
```

- [ ] **Step 4: Create src/pages/[locale]/blog/tags/[tag].astro**

```astro
---
import type { GetStaticPaths } from 'astro';
import Layout from '../../../../layouts/Layout.astro';
import Nav from '../../../../components/Nav.astro';
import BlogCard from '../../../../components/BlogCard.astro';
import TagList from '../../../../components/TagList.astro';
import { locales, type Locale } from '../../../../i18n/config';
import { useTranslations } from '../../../../i18n/ui';
import { getCollection } from 'astro:content';

export const getStaticPaths = (async () => {
  const paths = [];
  for (const locale of locales) {
    const posts = await getCollection('blog', ({ id, data }) => {
      return id.startsWith(`${locale}/`) && !data.draft;
    });
    const tags = new Set<string>();
    posts.forEach((post) => post.data.tags.forEach((tag) => tags.add(tag)));

    for (const tag of tags) {
      paths.push({
        params: { locale, tag },
        props: { tag },
      });
    }
  }
  return paths;
}) satisfies GetStaticPaths;

const { locale } = Astro.params as { locale: Locale };
const { tag } = Astro.props;
const t = useTranslations(locale);

const allPosts = await getCollection('blog', ({ id, data }) => {
  return id.startsWith(`${locale}/`) && !data.draft;
});

const posts = allPosts
  .filter((post) => post.data.tags.includes(tag))
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());

const allTags = allPosts.flatMap((post) => post.data.tags);

function getSlug(id: string): string {
  return id.replace(`${locale}/`, '').replace(/\.md$/, '');
}
---

<Layout
  title={`${t('blog.postsTagged')} "${tag}" — Eugen Soloviov`}
  description={t('blog.description')}
  locale={locale}
>
  <Nav locale={locale} />
  <main class="min-h-screen pt-24 pb-16">
    <div class="max-w-5xl mx-auto px-6">
      <header class="mb-10">
        <a href={`/${locale}/blog/`} class="text-sm text-gray-400 hover:text-violet-300 transition-colors">
          {t('blog.backToBlog')}
        </a>
        <h1 class="text-3xl sm:text-4xl font-bold text-white mb-3 mt-4">
          {t('blog.postsTagged')} <span class="text-violet-400">#{tag}</span>
        </h1>
        <p class="text-gray-500">{posts.length} {posts.length === 1 ? 'post' : 'posts'}</p>
      </header>

      <div class="mb-8">
        <TagList tags={allTags} locale={locale} activeTag={tag} />
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {posts.map((post) => (
          <BlogCard
            title={post.data.title}
            description={post.data.description}
            pubDate={post.data.pubDate}
            tags={post.data.tags}
            heroImage={post.data.heroImage}
            slug={`/${locale}/blog/${getSlug(post.id)}/`}
            readMoreLabel={t('blog.readMore')}
          />
        ))}
      </div>
    </div>
  </main>
  <footer class="border-t border-white/5 py-8 text-center text-sm text-gray-400">
    <p class="font-mono text-xs tracking-wider">{t('footer.tagline')}</p>
  </footer>
</Layout>
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/\[locale\]/blog/
git commit -m "feat: add blog pages (list, post, tags) with i18n routing"
```

---

## Task 12: Localized Portfolio Page + Root Redirect

**Files:**
- Create: `src/pages/[locale]/index.astro`
- Modify: `src/pages/index.astro`

- [ ] **Step 1: Create src/pages/[locale]/index.astro**

Copy the content of the current `src/pages/index.astro` but wrap it in the locale-aware Layout and Nav:

```astro
---
import type { GetStaticPaths } from 'astro';
import Layout from '../../layouts/Layout.astro';
import Hero from '../../components/Hero.astro';
import About from '../../components/About.astro';
import TechStack from '../../components/TechStack.astro';
import Projects from '../../components/Projects.astro';
import Contact from '../../components/Contact.astro';
import Nav from '../../components/Nav.astro';
import { locales, type Locale } from '../../i18n/config';
import { useTranslations } from '../../i18n/ui';

export const getStaticPaths = (() => {
  return locales.map((locale) => ({ params: { locale } }));
}) satisfies GetStaticPaths;

const { locale } = Astro.params as { locale: Locale };
const t = useTranslations(locale);
---

<Layout locale={locale}>
  <Nav locale={locale} />
  <main>
    <Hero />
    <About />
    <TechStack />
    <Projects />
    <Contact />
  </main>
  <footer class="border-t border-white/5 py-8 text-center text-sm text-gray-400">
    <p class="font-mono text-xs tracking-wider">{t('footer.tagline')}</p>
  </footer>
</Layout>
```

- [ ] **Step 2: Replace src/pages/index.astro with redirect**

```astro
---
// Redirect to default locale
return Astro.redirect('/en/');
---
```

- [ ] **Step 3: Commit**

```bash
git add src/pages/
git commit -m "feat: add locale-aware portfolio page, redirect root to /en/"
```

---

## Task 13: RSS Feed

**Files:**
- Create: `src/pages/[locale]/blog/feed.xml.ts`

- [ ] **Step 1: Create src/pages/[locale]/blog/feed.xml.ts**

```typescript
import type { APIContext, GetStaticPaths } from 'astro';
import rss from '@astrojs/rss';
import { getCollection } from 'astro:content';
import { locales, type Locale } from '../../../i18n/config';
import { useTranslations } from '../../../i18n/ui';

export const getStaticPaths = (() => {
  return locales.map((locale) => ({ params: { locale } }));
}) satisfies GetStaticPaths;

export async function GET(context: APIContext) {
  const locale = context.params.locale as Locale;
  const t = useTranslations(locale);

  const posts = await getCollection('blog', ({ id, data }) => {
    return id.startsWith(`${locale}/`) && !data.draft;
  });

  const sortedPosts = posts.sort(
    (a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime()
  );

  return rss({
    title: `${t('blog.title')} — Eugen Soloviov`,
    description: t('blog.description'),
    site: context.site!,
    items: sortedPosts.map((post) => ({
      title: post.data.title,
      pubDate: post.data.pubDate,
      description: post.data.description,
      link: `/${locale}/blog/${post.id.replace(`${locale}/`, '').replace(/\.md$/, '')}/`,
      categories: post.data.tags,
    })),
    customData: `<language>${locale}</language>`,
  });
}
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/\[locale\]/blog/feed.xml.ts
git commit -m "feat: add RSS feed per locale"
```

---

## Task 14: robots.txt + Tailwind RTL

**Files:**
- Create: `public/robots.txt`
- Modify: `tailwind.config.mjs`

- [ ] **Step 1: Create public/robots.txt**

```
User-agent: *
Allow: /

Sitemap: https://suenot.github.io/sitemap-index.xml
```

- [ ] **Step 2: Update tailwind.config.mjs**

Replace `tailwind.config.mjs` with:

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      colors: {
        accent: '#7C3AED',
        'accent-light': '#A78BFA',
        'surface': '#0F0F1A',
        'surface-2': '#1A1A2E',
        'surface-3': '#16213E',
        'muted': '#6B7280',
      },
      fontFamily: {
        sans: ['-apple-system', 'BlinkMacSystemFont', 'SF Pro Display', 'Segoe UI', 'Roboto', 'Helvetica Neue', 'Arial', 'sans-serif'],
        mono: ['SF Mono', 'Menlo', 'Cascadia Code', 'Consolas', 'Ubuntu Mono', 'monospace'],
      },
    },
  },
  plugins: [],
};
```

(Note: Tailwind 3.4 supports RTL natively via `rtl:` variant when `dir="rtl"` is on `<html>` — no config changes needed beyond what's already there.)

- [ ] **Step 3: Commit**

```bash
git add public/robots.txt tailwind.config.mjs
git commit -m "feat: add robots.txt, confirm Tailwind RTL support"
```

---

## Task 15: Build Verification

**Files:** None new

- [ ] **Step 1: Install dependencies and build**

```bash
cd /Users/suenot/projects/w_sdvg/w_suenot/suenot.github.io
bun install
bun run build
```

Expected: Build succeeds with no errors. Output in `dist/` contains locale directories.

- [ ] **Step 2: Verify output structure**

```bash
ls -la dist/
ls -la dist/en/
ls -la dist/en/blog/
ls -la dist/ru/
```

Expected: Directories for `en`, `ru`, `zh`, `ko`, `ja`, `ar` with blog subdirectories and HTML files.

- [ ] **Step 3: Fix any build errors and commit**

Fix any issues found during build, then:

```bash
git add -A
git commit -m "fix: resolve build issues for i18n blog"
```
